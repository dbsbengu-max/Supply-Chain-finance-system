# EA-032 试点上线包与运维闸门

生成日期：2026-05-31  
分支建议：`feat/ea-032-pilot-launch`（基于 EA-030/031 或 `master`）

## 1. 目标

为 **试点 prod** 提供可执行的上线包骨架，覆盖环境变量、数据库迁移、账号权限核查、日志审计、异常告警、UAT 操作手册与回滚方案。本阶段 **不改动业务代码**，仅交付 `deploy/pilot/` 目录与运维文档；已知风险（`DevDataInitializer`、mock seed）在闸门中显式标记。

## 2. 交付物索引

| 模块 | 路径 | 说明 |
|---|---|---|
| 总览 | [`deploy/pilot/README.md`](../deploy/pilot/README.md) | 上线顺序 1–7 |
| 环境变量 | [`deploy/pilot/.env.example`](../deploy/pilot/.env.example) | 全量清单 + 注释 |
| Spring prod | [`deploy/pilot/application-prod.example.yml`](../deploy/pilot/application-prod.example.yml) | `${ENV}` 占位，Flyway clean 禁用 |
| Flyway | [`deploy/pilot/flyway/PRODUCTION_STRATEGY.md`](../deploy/pilot/flyway/PRODUCTION_STRATEGY.md) | V1_1_001–026 策略 |
| Seed 核查 | [`deploy/pilot/scripts/verify-pilot-seed.sql`](../deploy/pilot/scripts/verify-pilot-seed.sql) | OP001/PJ001、4 用户、角色权限 |
| Seed 脚本 | [`deploy/pilot/scripts/verify-pilot-seed.ps1`](../deploy/pilot/scripts/verify-pilot-seed.ps1) | psql 封装 + mock_hash 告警 |
| 预检 | [`deploy/pilot/scripts/pre-flight.ps1`](../deploy/pilot/scripts/pre-flight.ps1) | health + seed + 可选 build/smoke |
| 日志审计 | [`deploy/pilot/logging/LOGGING_AUDIT.md`](../deploy/pilot/logging/LOGGING_AUDIT.md) | 应用日志 + `audit_operation_log` |
| Logback 样例 | [`deploy/pilot/logging/logback-spring.example.xml`](../deploy/pilot/logging/logback-spring.example.xml) | prod 滚动文件 |
| 告警 | [`deploy/pilot/monitoring/ALERTING_CHECKLIST.md`](../deploy/pilot/monitoring/ALERTING_CHECKLIST.md) | A-01–A-07 规则 |
| UAT | [`deploy/pilot/uat/UAT_OPERATION_MANUAL.md`](../deploy/pilot/uat/UAT_OPERATION_MANUAL.md) | 账号、smoke、EA-029 引用 |
| 回滚 | [`deploy/pilot/rollback/ROLLBACK_RUNBOOK.md`](../deploy/pilot/rollback/ROLLBACK_RUNBOOK.md) | 前端 / 应用 / DB |

## 3. 运维闸门（Go-Live Gate）

上线前 **全部勾选** 方可切换流量：

### G-ENV 环境

- [ ] `.env` 已从 `.env.example` 复制，**无**默认 dev 密钥
- [ ] `SCF_JWT_SECRET` ≥ 256 bit，与 staging 不同
- [ ] `SCF_DEV_PASSWORD_BOOTSTRAP=false` 或未设置
- [ ] `SCF_BANK_CALLBACK_TOKEN` 已替换 mock
- [ ] `SCF_FILE_STORAGE_DIR` 可写且已挂载/备份策略就绪

### G-DB 数据库

- [ ] 已执行 `pg_dump` 全量备份（见 rollback）
- [ ] Flyway `flyway_schema_history` 无 FAILED
- [ ] 最高版本 = 当前代码库 latest（026）
- [ ] 已阅读 V1_1_004 mock seed 风险并接受或计划 EA-033 拆分

### G-IAM 账号权限

- [ ] `verify-pilot-seed.ps1` 输出 PASS
- [ ] `platform_admin` 密码 **非** 仅依赖 `mock_hash`（须运维重置或 dev bootstrap 已关）
- [ ] 四角色 U001–U004 与 EA-013 权限码可登录验证

### G-OBS 可观测

- [ ] Health：`GET /api/v1/actuator/health` → UP
- [ ] 日志级别 INFO；logback 样例已评估是否落地
- [ ] 告警 A-01–A-07 接收人已配置（`.env` OPS 字段）
- [ ] Saga 监控台 MANUAL_REQUIRED = 0（或可解释）

### G-UAT 验收

- [ ] `pre-flight.ps1` → PRE-FLIGHT PASS
- [ ] EA-029 核心 UAT 24 条 + G1–G7 签字（见 UAT 手册）
- [ ] EA-031 smoke 6/6 PASS（`npm run smoke`）

### G-RBK 回滚

- [ ] 上一版 frontend jar/dist 与 backend jar 已归档
- [ ] DB 备份路径与恢复命令已演练（staging）

## 4. 推荐上线顺序

```text
1. 备份 DB + 归档当前制品
2. 注入 .env / application-prod.yml
3. 启动 backend（Flyway auto-migrate）
4. verify-pilot-seed.ps1
5. 部署 frontend（nginx → SCF_API_UPSTREAM）
6. pre-flight.ps1
7. UAT 手册签字
8. 切换流量 / 公告
```

## 5. 与 EA-029 / EA-031 关系

| 前置 | EA-032 消费点 |
|---|---|
| EA-029 G1–G7 | UAT 手册引用 24 条用例与闸门判据 |
| EA-031 smoke | pre-flight 可选步骤；试点期每日回归 |
| EA-030 深链 | UAT 代采/Saga/清分/BI 导航路径 |
| EA-013 权限 | seed SQL 核查 `permission` / `role_permission` |

## 6. 已知风险与 EA-033  backlog

| ID | 风险 | 缓解（本阶段） | 后续 |
|---|---|---|---|
| R-01 | `DevDataInitializer` 无条件运行 | 文档 + `SCF_DEV_PASSWORD_BOOTSTRAP=false` | `@Profile("!prod")` |
| R-02 | V1_1_004 含演示业务数据 | Flyway 文档标记 | 拆 seed_iam / seed_demo |
| R-03 | 无外部 APM | 告警清单 + 人工 Saga 巡检 | Webhook / Prometheus |
| R-04 | 仓库无正式 `application-prod.yml` | 仅 example | 密钥管理接入 |

## 7. 快速命令

```powershell
# 环境（PowerShell）
cd deploy\pilot
Copy-Item .env.example .env   # 编辑后

# Seed 核查
.\scripts\verify-pilot-seed.ps1 -DbHost localhost -DbName scf -DbUser scf

# 全量预检（需 backend + 可选 frontend smoke）
.\scripts\pre-flight.ps1 -BackendUrl http://127.0.0.1:8080/api/v1
```

```bash
# 健康检查
curl -s http://127.0.0.1:8080/api/v1/actuator/health
```

## 8. Codex 下一轮建议

1. 在 **staging** 跑通 pre-flight + verify-pilot-seed，贴输出到验收结果 doc  
2. 确认 prod 是否保留 V1_1_004 演示数据 → 影响 UAT 用例范围  
3. 实现 `DevDataInitializer` prod 禁用或 profile 隔离  
4. 将 logback-spring.example.xml 合入主工程并验证滚动日志  
5. 告警 A-03/A-04 脚本化（Saga summary cron）

---

**状态：** 骨架已交付，待 staging 演练与签字。
