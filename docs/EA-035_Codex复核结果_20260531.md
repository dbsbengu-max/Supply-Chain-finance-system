# EA-035 Codex复核结果

复核日期：2026-05-31  
复核范围：EA-035 Staging真实验证与发布签核、pilot脚本、Flyway迁移、后端回归、前端生产构建、本地健康闸门。

## 1. 复核结论

| 项目 | 结论 | 说明 |
|---|---|---|
| 代码与脚本层 | Conditional Pass | 已具备进入真实 staging 跑闸的基础 |
| 本地后端回归 | Pass | `EA019RegressionRunner`：113/113 通过 |
| 前端生产构建 | Pass | `npm run build` 通过 |
| pilot脚本语法 | Pass | `deploy/pilot` 下 12 个 PowerShell 脚本解析通过 |
| 本地健康闸门 | Pass | A-01 health=UP，A-03/A-04 本地跳过 |
| 真实发布签核 | No-Go | 真实 staging 尚未完成 gate/watch/Go-No-Go 签字 |

最终判定：**EA-035 可以交给运维在真实 staging 环境执行签核跑闸；当前不能宣称可发布生产。**

## 2. 本次发现并修正

| 问题 | 风险 | 处理 |
|---|---|---|
| `V1_1_027__seed_profile_manifest.sql` 残留 PostgreSQL `ON CONFLICT` | H2/Flyway 本地回归或非 PG 兼容验证失败 | 改为 `AND NOT EXISTS` 幂等写法 |
| 构建产物曾掩盖源 SQL 问题 | 可能出现 target 可跑、源码不可复现 | 重新复制源码 migration 后回归 |

## 3. 验证记录

| 验证项 | 命令/方式 | 结果 |
|---|---|---|
| 后端编译 + 回归 | 手动 `javac` 编译后执行 `com.scf.EA019RegressionRunner` | Pass，113/113 |
| Flyway迁移 | 测试启动自动迁移 | Pass，成功迁移至 `v1.1.027` |
| 前端构建 | `npm run build` | Pass |
| pilot脚本语法 | PowerShell Parser 解析 `deploy/pilot/**/*.ps1` | Pass，12个脚本 |
| 日志/迁移风险扫描 | 生产日志配置、Flyway migration 兼容性、占位符检查 | Pass，核心配置与迁移无阻断项 |
| 本地 A-01 | `check-pilot-alerts.ps1 -BackendUrl http://localhost:8080/api/v1/actuator/health -SkipSaga` | Pass，health=UP |

后端回归证据：`backend/scf-server/target/ea035-regression-result.txt`  
前端构建说明：存在 Element Plus 大 chunk warning，属于后续性能优化项，不阻断 EA-035。

## 4. 仍需真实 staging 完成的阻断项

| 阻断项 | 验收标准 |
|---|---|
| staging-gate | `run-ea035-signoff.ps1` 在真实 `.env` 下 PASS |
| Flyway版本 | staging 数据库迁移到 `>= 1.1.027` |
| seed校验 | 四用户、关键权限、`sys_seed_manifest` 均通过 |
| A-01 | backend health 从跳板机稳定返回 `UP` |
| A-03 | `biz_event_outbox FAILED=0`，stale backlog 达标 |
| A-04 | `biz_compensation_task MANUAL_REQUIRED=0` 或完成业务确认 |
| alerts-watch | 30分钟窗口 PASS；生产前建议补 24小时观察 |
| Go/No-Go | `GO_NO_GO_CHECKLIST.md` 完成业务、技术、运维签字 |

## 5. 给 Cursor/运维的下一步

1. 在真实 staging 跳板机填充 `deploy/pilot/.env`，不得含 `CHANGE_ME`。
2. 执行：

```powershell
cd deploy\pilot
.\scripts\run-ea035-signoff.ps1 -WatchMinutes 30 -WatchIntervalMinutes 5
```

3. 若 30 分钟 watch PASS，补跑 24 小时观察：

```powershell
.\scripts\run-ea035-signoff.ps1 -WatchHours 24 -WatchIntervalMinutes 15
```

4. 将生成的 staging evidence、`GO_NO_GO_CHECKLIST.md`、UAT签字材料归档。

## 6. 复核裁决

**当前状态：Conditional Pass / Release No-Go。**

代码、迁移和签核脚本已具备真实 staging 验证条件；生产发布必须等待真实 staging 证据闭环，不能用本地 smoke 替代。
