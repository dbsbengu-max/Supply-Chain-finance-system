# EA-046 供应商 Sandbox 联调证据包验收结果

**日期**：2026-06-01  
**范围**：供应商 Sandbox 证据包工具链 + 本地 quasi-sandbox 闭环 + 可审计证据包

## 结论

| 项 | 结果 |
|---|---|
| 自动化证据脚本 | `run-ea046-sandbox-evidence.ps1` |
| 证据 JSON schema | `deploy/pilot/evidence/contract-sign-sandbox/ea046-evidence.schema.json` |
| 单元/回归基线 | EA-019 **165/165**（EA-045） |
| 本地 quasi-sandbox 闭环 | ✅ **PASS**（HttpEsignVendorStub，2026-06-01） |
| 外部真实供应商 Sandbox | ☐ 待执行 — 替换真实供应商 endpoint/appId/密钥后复跑 |

## 沙箱 run 记录

| 字段 | 值 |
|---|---|
| run_id | `ea046-20260601-105349` |
| vendor | `HttpEsignVendorStub`（local quasi-sandbox；外部沙箱需替换为真实供应商名） |
| doc_version | `EA-046-local` |
| external_sign_ref | `HTTP-FLOW-2e28f5444c6e40f3b20c989923d217be` |
| provider_request_id | `VREQ-2e28f5444c6e40f3b20c989923d217be` |
| provider_trace_id | `VTR-2e28f5444c6e40f3b20c989923d217be` |
| platform_trace_id | `ea046-20260601-105349-INITIATE` |
| verdict | **PASS** |
| signed_off_by | `local-sandbox-runner` |
| evidence_path | `deploy/pilot/evidence/contract-sign-sandbox/ea046-20260601-105349/` |

## 场景结果（12/12 PASS）

HEALTH → LOGIN → CONFIG → INITIATE → LOOKUP → QUERY_STATUS → CALLBACK_REPLAY → CALLBACK_IDEMPOTENT → COMPENSATION_UNKNOWN_REF → COMPENSATION_RETRY → SAGA_QUERY_SIGN_STATUS → DB_TRACE_EXPORT

## Codex 复核记录

| 检查项 | 结果 | 说明 |
|---|---|---|
| 证据 JSON | PASS | `ea046-20260601-105349.json`，`verdict=PASS`，12 个场景全通过 |
| Trace 留痕 | PASS | DB 导出包含 `platform_trace_id`、`provider_request_id`、`provider_trace_id` |
| 回调幂等 | PASS | `callback-idempotent-response.json` 返回 `idempotent_replay=true` |
| 证据脱敏 | PASS | `login-response.json` 中 accessToken 已脱敏 |
| 供应商真实性 | PARTIAL | 当前为本地 `HttpEsignVendorStub`，尚未证明外部真实供应商 Sandbox 可用 |

## 执行命令（本地 quasi-sandbox）

```powershell
cd deploy\pilot
.\scripts\run-ea046-local-sandbox.ps1
```

真实供应商沙箱：

```powershell
copy .env.esign-sandbox.example .env.esign-sandbox
# 填写供应商沙箱密钥
.\scripts\run-ea046-sandbox-evidence.ps1 -EnvFile .\.env.esign-sandbox
```

## 备注

- 回调 HMAC 载荷须与 Jackson 序列化一致（含 `failure_reason:null`）；已修复证据脚本。
- 生产切换见 **EA-047** 灰度策略与 `run-ea047-prod-cutover-gate.ps1`。
- 真实供应商 Go/No-Go 见 **EA-048** `run-ea048-real-vendor-gonogo.ps1`（GO 后才 ALLOWLIST）。
