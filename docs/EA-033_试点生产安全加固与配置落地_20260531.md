# EA-033 试点生产安全加固与配置落地

生成日期：2026-05-31

## 1. 目标

在 EA-032 上线包骨架基础上，完成 **prod 安全默认值**、**配置落地**、**告警脚本化**、**staging 证据归档** 与 **Flyway seed 决策**。

## 2. 交付清单

| 项 | 状态 | 位置 |
|---|---|---|
| 禁用 prod `DevDataInitializer` | ✅ | `@Profile("!prod")` + `scf.dev.password-bootstrap` |
| `application-prod.yml` 落地 | ✅ | `backend/scf-server/src/main/resources/application-prod.yml` |
| staging seed 核查 + 归档 | 🔧 脚本就绪 | `verify-pilot-seed.ps1 -ArchiveDir` |
| V1_1_004 决策 | ✅ 保留 | `deploy/pilot/flyway/V1_1_004_DECISION.md` |
| A-01 / A-03 / A-04 脚本化 | ✅ | `deploy/pilot/monitoring/check-*.ps1` |
| Logback 挂载评估 | ✅ | `deploy/pilot/logging/LOGBACK_MOUNT_EVAL.md` |

## 3. DevDataInitializer 行为

| Profile | `scf.dev.password-bootstrap` | 行为 |
|---|---|---|
| dev（默认） | true（默认） | 将 mock_hash 更新为演示密码 |
| dev / staging | false | 不运行 |
| **prod** | 任意 | **不运行**（`@Profile("!prod")`） |

测试：`DevDataInitializerProdTest`

## 4. Staging seed 归档

```powershell
cd deploy\pilot
# .env 指向 staging PostgreSQL
.\scripts\verify-pilot-seed.ps1 -ArchiveDir .\evidence\staging
```

日志写入 `deploy/pilot/evidence/staging/seed-verify-*.log`，纳入发布审计。

> **本机阻塞：** 当前 agent 环境无 `psql` / Docker，staging 需在目标机执行后提交 log 或贴 ticket。

## 5. V1_1_004 决策摘要

**试点保留** 全量 mock seed（OP001/PJ001、四用户、演示企业/订单），以满足 EA-029 UAT 与 smoke。prod 须重置密码；空业务库拆分留 **EA-034**。

## 6. 告警脚本

```powershell
cd deploy\pilot\monitoring
.\check-pilot-alerts.ps1 -BackendUrl http://staging:8080/api/v1/actuator/health
# Cron 示例（每 5 分钟）:
# pwsh -File check-pilot-alerts.ps1 -StrictStale
```

| ID | 脚本 | 退出码 |
|---|---|---|
| A-01 | `check-a01-health.ps1` | 2 = not UP |
| A-03 | `check-saga-alerts.ps1` | 3 = FAILED（`-StrictStale` 含 30min） |
| A-04 | `check-saga-alerts.ps1` | 4 = MANUAL_REQUIRED |

## 7. Logback

试点 **推荐外部挂载** `SCF_LOGBACK_CONFIG=file:/etc/scf/logback-spring.xml`，详见 `LOGBACK_MOUNT_EVAL.md`。未设置时 prod 仍可用默认控制台 + 容器日志采集。

## 8. 上线前增量闸门（EA-033）

- [ ] `SPRING_PROFILES_ACTIVE=prod` 启动，日志无 “Updated dev password”
- [ ] `DevDataInitializerProdTest` / CI 通过
- [ ] staging `verify-pilot-seed.ps1 -ArchiveDir` 已归档
- [ ] `check-pilot-alerts.ps1` 在 staging cron 试跑 24h
- [ ] 四用户密码已重置（非 mock_hash）

## 9. EA-034 backlog

- Flyway 拆分 IAM / demo seed
- A-02 / A-05–A-07 脚本或 Prometheus
- Logback JSON + 企微 Webhook
- `pre-flight.ps1` 集成 `check-pilot-alerts.ps1`

---

**状态：** 代码与脚本已落地；staging 证据待目标环境执行。
