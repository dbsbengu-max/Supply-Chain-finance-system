# EA-049 功能上线收口验收结果

**日期**：2026-06-01  
**范围**：Cursor 完成的 EA-049 功能上线收口、试点演示闭环、UAT 最小集与阻断清单；Codex 执行后端回归、前端 build、前端 smoke 复核。

## 1. 结论

| 项 | 结果 |
|---|---|
| 后端回归 | PASS：171/171 |
| 前端 build | PASS |
| 前端 smoke | PASS：9/9 |
| 功能上线阻断 | 暂无 BLOCKER-L |
| 真实生产阻断 | 保留为后置待办，不阻塞 EA-049 |
| **Verdict** | **GO demo / GO 功能上线收口** |

## 2. 执行记录

| 检查项 | 命令 / 方式 | 结果 |
|---|---|---|
| 后端回归 | `EA019RegressionRunner` | `RESULT tests=171 succeeded=171 failed=0 skipped=0` |
| Flyway 迁移 | 回归启动 H2 test profile | 成功应用 34 个迁移，至 `v1.1.034` |
| 前端 build | `npm run build` | PASS；仅保留 Vite 大 chunk 提示 |
| 前端 smoke | 手动启动 backend test profile + Vite，`SMOKE_SKIP_WEBSERVER=1 npx playwright test tests/smoke/pilot-routes.spec.ts --config=playwright.config.ts --reporter=list` | 9 passed |
| 后端 health | `GET /api/v1/actuator/health` | `UP` |

## 3. 本轮修复

| 文件 | 问题 | 处理 |
|---|---|---|
| `frontend/scf-web/src/views/SagaOpsCenterView.vue` | 模板使用 `router.push`，脚本未实例化 `router`，导致 `vue-tsc` 失败 | 补充 `const router = useRouter()` |
| `frontend/scf-web/tests/smoke/pilot-routes.spec.ts` | Element 菜单点击在 smoke 中偶发未触发路由，导致后续串行用例跳过 | `openMenu` 增加按菜单名映射的路由兜底 |
| `docs/EA-049_功能上线阻断与真实生产阻断清单_20260601.md` | L-06/L-07 仍为待跑 | 更新为 OK |

## 4. Smoke 明细

| ID | 场景 | 结果 |
|---|---|---|
| SMOKE-01 | 登录 | PASS |
| SMOKE-02 | 试点闭环 | PASS |
| SMOKE-03 | 补偿池 | PASS |
| SMOKE-04 | 融资管理 | PASS |
| SMOKE-05 | 清分中心 | PASS |
| SMOKE-06 | 经营看板 | PASS |
| SMOKE-07 | 功能上线收口 | PASS |
| SMOKE-08 | 签章中心 | PASS |
| SMOKE-09 | 客户/KYC | PASS |

## 5. 仍需关注

| 类型 | 项 | 说明 |
|---|---|---|
| WARN | 前端大 chunk | `element-plus` chunk 约 794 kB，`echarts` 约 381 kB；当前不阻断，后续可继续按路由拆包/按需加载优化 |
| WARN | `npm run smoke` 默认 webServer 模式在本机 Windows 曾出现命令不退出 | 可使用本次验证的手动启动方式，或后续把 smoke 脚本封装为稳定 PowerShell |
| 后置 | EA-048 真实 vendor Go/No-Go | 仍为真实生产/供应商切换前置待办，不阻塞 EA-049 |

## 6. 当前判断

EA-049 已达到“可用、可测、可演示、可交付”的 Mock/试点功能上线标准。下一步可以进入人工 UAT 签字和功能细节增强；真实 vendor、真实银行、生产灰度继续按 EA-048/EA-047 后置执行。
