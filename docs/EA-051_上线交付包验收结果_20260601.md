# EA-051 上线交付包验收结果

日期：2026-06-01  
执行人：Codex  
范围：上线交付包索引、用户手册、演示脚本、测试账号矩阵、已知限制、开发冻结清单、一键验收脚本

## 1. 结论

EA-051 已达到“Mock/试点功能上线交付包可执行”标准。

本次执行 `run-ea051-acceptance.ps1` 后，A0-A4 门禁通过：PostgreSQL 可用、后端健康、后端回归 171/171、前端 build、前端 smoke 9/9。

真实供应商 Sandbox、真实银行、生产灰度 Go/No-Go 仍按 EA-048/EA-047 后置，不纳入本次交付包上线口径。

## 2. 执行结果

执行目录：

```text
E:\cursor\BR PT\供应链金融管理系统\deploy\pilot\scripts
```

执行命令：

```powershell
.\run-ea051-acceptance.ps1
```

证据文件：

```text
E:\cursor\BR PT\供应链金融管理系统\deploy\pilot\evidence\ea051-acceptance\ea051-20260601-151631.summary.txt
```

结果摘要：

| 门禁 | 结果 | 说明 |
|---|---:|---|
| A0 PostgreSQL | PASS | `127.0.0.1:5432` 可连接 |
| A4 Backend Health | PASS | `GET /api/v1/actuator/health -> UP` |
| A1 Backend Regression | PASS | `EA019RegressionRunner 171/171` |
| A2 Frontend Build | PASS | `npm run build` 通过 |
| A3 Frontend Smoke | PASS | `smoke 9/9` |

最终输出：

```text
>>> EA-051 ACCEPTANCE: PASS <<<
```

## 3. 修正记录

本次复核发现一键验收脚本在本机无完整 Maven 离线依赖时会误失败。已修正：

1. `Find-Mvn` 不再直接抛错，允许无 Maven 时进入本地 classpath fallback。
2. Maven 执行失败且已有 `target/cp-jars` 时，降级使用既有回归 classpath。
3. 修正 fallback classpath，显式包含 `target/classes` 与 `target/test-classes`，确保 `com.scf.EA019RegressionRunner` 可加载。

涉及文件：

```text
E:\cursor\BR PT\供应链金融管理系统\deploy\pilot\scripts\run-ea051-acceptance.ps1
```

## 4. 当前交付状态

| 交付物 | 状态 |
|---|---:|
| 用户手册 | PASS |
| 演示脚本 M1-M12 | PASS |
| 测试账号矩阵 | PASS |
| 已知限制清单 | PASS |
| 开发冻结清单 | PASS |
| UAT 页面入口 `/uat/acceptance` | PASS |
| 一键验收脚本 | PASS |

## 5. 后续建议

建议下一阶段进入 EA-052：UAT 现场签字与交付归档。

EA-052 的目标不是继续加功能，而是把业务验收动作闭环：

| 子项 | 内容 |
|---|---|
| EA-052-A | 按 `DEMO_SCRIPT_M1-M12.md` 逐项走演示 |
| EA-052-B | 在 `/uat/acceptance` 标记 M1-M12 状态并填写备注 |
| EA-052-C | 导出签字包并归档到 `deploy/pilot/evidence/uat-signoff` |
| EA-052-D | 记录业务方问题清单，按 P0/P1/P2 分类 |
| EA-052-E | 若无 P0，给出 Mock/试点 GO trial 建议 |

## 6. 上线口径

可对外使用以下口径：

> 系统已完成 Mock/试点功能上线交付包，自动化回归与 smoke 均通过，可进入业务 UAT 签字和试点演示。真实供应商、真实银行与生产灰度仍为后置专项，不影响当前 Mock/试点功能验收。
